name: master_piece_puzzle
description: master_piece_puzzle

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: '>=2.18.4 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  image: ^3.2.2
  path_provider: ^2.0.11
  provider: ^6.0.0
  logger: ^1.1.0
  google_mobile_ads: ^2.3.0
  shared_preferences: ^2.0.15


dev_dependencies:
  flutter_test:
    sdk: flutter
  google_mobile_ads: ^2.3.0
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/
    - assets/data.json

